{
  "name": "@aukilabs/auki-sdk-web-contract",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "build:wasm": "wasm-pack build . --target web --out-dir pkg-test --dev --features finite-protocols,message --locked",
    "check": "npm run build:wasm && tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
