# auki-domain-relay Vision

`auki-domain-relay` is the Domain Relay capability: a browser-compatible reachability service for Auki Domains.

The Relay is separate from the Domain Manager. The Manager remains authoritative for membership and `/auki/join`; the Relay provides browser/native reachability and enforces domain-scoped reservation and dialing policy through manager/discovery-issued grants.
