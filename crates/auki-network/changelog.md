# Changelog — auki-network

Append-only changelog for this crate. See [CLAUDE.md](../../CLAUDE.md) for the format and propagation rules.

Latest entry on top.

---

### broodsugar's claude · May 4, 16:30 HKT, 2026

`app_instance::derive()` landed — implementation of ansuz #5, the per-machine identifier that distinguishes two daemons of the same `app` running on different hardware. New `auki_network::app_instance` module behind a default-off `app_instance` feature (non-WASM by design — depends on the `mac_address` crate, which uses platform syscalls `getifaddrs` / `GetAdaptersAddresses`). Recipe locked per ansuz D4: enumerate interfaces, skip loopback (all-zero MAC) and locally-administered MACs (U/L bit `0x02` on the first octet), sort the remaining MACs lexicographically by raw bytes, render the smallest as 12 lowercase hex chars without separators (`aabbccddeeff`). Sorting (rather than relying on OS interface enumeration order) gives a stable selection across platforms and reboots for a fixed hardware set. `DeriveError` enum: `NoNetworkInterfaces` (empty enumeration), `NoSuitableMac` (everything filtered — typical in containers), `Io(std::io::Error)` (syscall failure). Public API: `derive() -> Result<String, DeriveError>` for production; `derive_from(macs: &[[u8; 6]]) -> Result<String, DeriveError>` as the testing seam (pure, no side effects). 9 new tests covering the locked `[0x00,0x16,0x3e,0xab,0xcd,0xef]` → `"00163eabcdef"` cross-language vector, schema (12 lowercase hex), `NoNetworkInterfaces` on empty, `NoSuitableMac` on loopback-only / locally-administered-only, IEEE selection skipping each filtered category, lexicographic-first ordering across multiple IEEE MACs, and the U/L-bit logic in isolation. The value this function returns is what PR #25's `/api/info` redesign will carry in its new `app_instance` field; the two PRs are independent. Stability caveats documented (containers, VMs, multi-NIC, MAC randomization) — fragile in those environments by ansuz acceptance; a stable-id alternative (wallet-derived, persisted) is parked for after ansuz. New parking-lot items: container/Docker handling, multi-NIC tiebreaker semantics, eventual stable-id options.

### broodsugar's claude · May 4, 14:30 HKT, 2026

`cluster.json` discovery-doc spec + loader landed (ansuz milestone deliverable #1). New always-on `cluster_doc` module sits alongside the M0 data types — no feature gate, native-only (uses `std::fs`); the rest of the crate's M0 path stays WASM-friendly because Console doesn't import this module. `ClusterDoc` carries `{version: u32, cluster_name: String, peers: Vec<ClusterPeer>}`; each `ClusterPeer` is `{peer_id: libp2p_identity::PeerId (required), addresses: Vec<multiaddr::Multiaddr>, expected_app_id: Option<String>, note: Option<String>}` — `peer_id` required per ansuz D1 (strict, libp2p Noise rejects mismatches at connection time so the doc gives identity continuity across daemon restarts), `expected_app_id` advisory only (authoritative `app_id` rides over the wire from `/api/info`). `LoadError` enum: `Io`, `Parse`, `UnsupportedVersion(u32)`, `InvalidPeerId(String)`, `InvalidMultiaddr(String)` — typed-field errors carry the offending value so an operator can fix the doc from the message alone; manual `Display` + `Error` impls (no `thiserror` dep on the M0 path). `load(&Path) -> Result<ClusterDoc, LoadError>` does a two-phase parse: peeks at `version` first so a future v2 doc with a restructured shape still surfaces `UnsupportedVersion` cleanly instead of an opaque `Parse`. Path layout: `<app_root>/registries/cluster_registries/cluster.json` — flat (single file, not hash-keyed like the existing `sensors/` / `clocks/` / `frames/` siblings); `default_path(app_root)` returns the canonical join, `resolve_path(app_root, cli_override)` honours `cli_override > $AUKI_CLUSTER_DOC > default` (empty env value treated as unset; the override wins over an env var when both are set). 16 new unit tests covering the full schema + every `LoadError` variant + path-resolution precedence + an empty-env-as-unset edge case (env-mutating tests serialize on a process-wide `OnceLock<Mutex>` to keep them parallel-safe alongside the rest of the suite). 3 integration tests in `tests/cluster_doc.rs` exercise the daemon-startup flow end-to-end: `resolve_path` → on-disk JSON → `load` at the default layout, the same with a CLI override, and the operator-typo path returning `InvalidPeerId` with the offending value. New `tempfile` dev-dep. Test counts now 35 unit + 3 integration + 1 doctest with `--features swarm` (was 19 + 0 + 1); 27 unit + 3 integration without features (was 11 + 0 + 0). M0 build with no features still clean. Empty-`addresses` peers are explicitly allowed at parse time (operator may temporarily de-route a pinned peer); consumers that need a dialable address should filter such entries out. `cluster_registries/` is intentionally **flat** for ansuz — lifting the cluster doc into a Cluster Registry primitive (hash-keyed, signed, etc.) is a future evolution, parking-lot item added. The doc is also unsigned for ansuz; cryptographic attestation of the cluster membership list is a future concern. Implements ansuz D1 (strict — peer_id required); D2/D3/D4/D5/D6 unaffected by this PR.

### broodsugar's claude · May 2, 18:45 HKT, 2026

M1b landed — Circuit Relay v2 + libp2p mDNS, plus a `dial_peer` helper for Park-from-home circuit-relay dialing. Encodes the three resolved Reid milestone-2 parking-lot answers (1a / 2c / 3c, all decided 2026-05-02). New libp2p features enabled: `mdns`, `relay`. `Behaviour` extended with `Toggle<mdns::tokio::Behaviour>`, `relay::client::Behaviour` (always wired into the transport stack via `with_relay_client`), and `Toggle<relay::Behaviour>`. `SwarmConfig` gains `enable_mdns: bool` (default true — daemons advertise on `_p2p._udp.local.` alongside their existing `_auki._tcp.local.` advertisement; tests opt out to avoid LAN noise) and `enable_relay_server: bool` (default false — consumer daemons don't relay; the dedicated `aukilabs/relay` app sets it true). `Capability::*` advertisement in `ReachabilityRecord` is independently gated by consumers — both knobs must line up for the peer to actually serve the capability. New `swarm::dial_peer(&mut Swarm, PeerId, Vec<Multiaddr>)` helper takes direct or circuit-relay multiaddrs (e.g. `/p2p/<relay>/p2p-circuit/p2p/<target>`); Park-from-home is operator-paste of `(peer-id, [optional relay multiaddr])` for the M2 demo. 4 new tests (build-with-mdns, build-with-relay-server, full reservation flow client→relay→ReservationReqAccepted, dial_peer helper); 19 unit tests + 1 doctest total. Reservation test required `add_external_address` on the relay swarm so its loopback listen address gets included in reservation responses (real networks learn external addresses via identify or AutoNAT). All resolved Propagate items removed from `parking_lot.md`; remaining items (peer-derivation label, `ReachabilityRecord` extensibility, `SwarmConfig` knob minimalism, `BuildError::Transport` structure) are forward-looking, not M1-blocking. New parking-lot items added: loopback `add_external_address` workaround note, DCUtR/hole-punching as future work. Layer 2 (capability advertisement / discovery via libp2p `request_response`) is the next chunk.

### broodsugar's claude · May 2, 17:30 HKT, 2026

M1a landed — libp2p `Swarm` builder behind the `swarm` feature. Composes `libp2p::tcp` + `libp2p::quic` transports under `libp2p::noise` (authenticated with the peer's ed25519 keypair) and `libp2p::yamux` multiplexing, with a minimal `identify` + `ping` `NetworkBehaviour`. New `auki_network::swarm` module exposes `build_swarm(&PeerIdentity, SwarmConfig) -> Result<libp2p::Swarm<Behaviour>, BuildError>`; the constructed swarm has its `local_peer_id` matching `identity.peer_id()` and is already listening on every `SwarmConfig.listen_addresses` entry before returning. Identify protocol id is `/auki/identify/1.0.0` (stable wire format); `agent_version` is the per-deployment knob. Idle connections close after 60s (ping resets the timer). 4 new tests + 1 doctest covering `build_swarm` correctness, mutual `identify::Event::Received` round-trip over TCP, the same over QUIC, and listening on multiple addresses simultaneously. `swarm` is **default-off** so the M0 path stays WASM-compilable for Console (which uses `default-features = false`); enabling the feature pulls in `libp2p` 0.56 (features: `tokio`, `tcp`, `quic`, `noise`, `yamux`, `identify`, `ping`, `macros`, `ed25519`) and `thiserror`. Workspace test count: 11 → 15 unit + 1 doctest. Next: M1b adds Circuit Relay v2 (`libp2p::relay` client always, server wrapped in `Toggle` for consumer-daemon off-by-default) and `_p2p._udp.local.` mDNS coexistence — both blocked on the parking-lot questions resolving first.

### broodsugar's claude · May 2, 16:10 HKT, 2026

Crate created — Layer 1 of the Reid milestone-2 networking stack. Ships the data types only: `PeerIdentity` (libp2p ed25519 keypair derived from a wallet via `derive_child("peer/v1")`, with `peer_id()` / `public_key()` / `keypair()` accessors), `ReachabilityRecord` (peer id + multiaddrs + capabilities + last-seen, JSON-serializable wire shape for peer discovery), and `Capability` (namespaced-string newtype with the four canonical `networking:*` constants from the Reid milestone-2 architecture: `MESSAGE_FORWARDING`, `BULK_DATA_CHANNEL`, `TURN`, `SFU`). 11 tests covering determinism of derivation, the public `from_wallet ≡ from_seed(derive_child("peer/v1").seed())` contract, JSON round-trips, and capability namespace extraction. WASM-friendly (`libp2p-identity` + `multiaddr` both compile to WASM); deliberately split from M1's libp2p `Swarm` so Console can derive a peer id from an in-browser wallet without pulling in the transport stack. Built on `auki-identity`, `libp2p-identity` 0.2 (`ed25519` + `peerid` + `serde` features, `default-features = false`), and `multiaddr` 0.18 (with a small local serde adapter — `multiaddr` 0.18 dropped its serde feature). M1 will add the libp2p `Swarm` (TCP/QUIC + Noise + Yamux + Circuit Relay v2) on top of these primitives. Parking-lot items: mDNS coexistence (`_p2p._udp.local.` vs `_auki._tcp.local.`); Wallet→peer-key derivation label evolution; Park-from-home access pattern; off-by-default relay-server plumbing; `ReachabilityRecord` extensibility / versioning. No tag yet — wait until M1 lands or the Relay app earns it.
