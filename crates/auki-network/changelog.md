# Changelog — auki-network

Append-only changelog for this crate. See [CLAUDE.md](../../CLAUDE.md) for the format and propagation rules.

Latest entry on top.

---

### broodsugar's claude · May 8, 10:11 HKT, 2026

**Doc cleanup after Step 2.** [`README.md`](README.md) and [`src/readme.md`](src/readme.md) caught up with the post-PR-#62 reality: stream-protocol section now describes the prost wire format (length-prefixed `StreamMessage` envelope), the framing helpers as non-generic over `T` (taking `StreamMessage`, with `T`'s prost bytes carried inside `Frame.payload`), the `T` bound on `stream_runtime::open_stream<T>` as `prost::Message + Default + Send + 'static`, and the locked test list reflecting the actual current tests (renames + 3 new locked vectors for `JpegFrame` / `PointCloudFrame` / full envelope). Producer-side example uses the current `StreamDispatch` + helper-constructor shape (`DeclineReason::sensor_not_found()` etc.). Dependencies section adds `auki-datatypes` and `prost` under the `swarm` feature. Doc-only.

### broodsugar's claude · May 8, 09:42 HKT, 2026

**Step 2 of the [`auki-datatypes` migration](../auki-datatypes/src/sprint.md): stream wire moves to protobuf.** [`stream_protocol`](src/stream_protocol.rs) replaces JSON-via-`serde_json` with prost-encoded `StreamMessage` from [`auki-datatypes`](../auki-datatypes)'s new `auki.stream` package. The framing primitive (4-byte BE u32 length prefix) is unchanged; the envelope and payload move to protobuf. Drops the `base64_bytes` serde adapter on `PointCloudFrame.bytes` — protobuf's `bytes` is native binary, no JSON-array-of-integers tax to dodge. Drops the `base64` dep from the `swarm` feature.

**Workspace 1.0.0 → 0.0.1 protocol-id rename.** Per Nils's "save 1.0.0 for the first official release" stance: `/auki/cluster/1.0.0` → `/auki/cluster/0.0.1` (no wire change, just docstrings + `CLUSTER_PROTOCOL` constant); `/auki/identify/1.0.0` → `/auki/identify/0.0.1` (same). The legacy JSON `/auki/stream/1.0.0` is **retired** — replaced by the protobuf `/auki/stream/0.1.0` in the same PR; consumers (Park, Boosterapp) update their decoders in lockstep.

**Stream protocol surface.** `pub use auki_datatypes::frame_stream::JpegFrame; pub use auki_datatypes::point_cloud_stream::PointCloudFrame; pub use auki_datatypes::stream::{StreamMessage, StreamRequest, AcceptInfo, DeclineReason, EndReason, Frame, decline_reason, end_reason, stream_message};`. The hand-rolled serde structs and the local `base64_bytes` module are gone. `StreamProtocolError` swaps `Serialize/Deserialize` variants for `Encode(prost::EncodeError) / Decode(prost::DecodeError)`; new `MissingVariant` for empty-oneof envelopes.

**`stream_runtime` `T` bound** changes from `Serialize + DeserializeOwned + Send + 'static` to `prost::Message + Default + Send + 'static`. `pump_typed` encodes each producer-side `T` to `Vec<u8>` for `Frame.payload`; `consumer_reader_task` decodes `Frame.payload` bytes back into `T`. Both go through `T::encode_to_vec()` / `T::decode(&[u8])`. Internal logic (handshake, mono-T per substream, EndOfStream lifecycle, shutdown grace) unchanged.

**Test counts**: `auki-network` 99 → 102 (+3 — three new locked vectors for `JpegFrame`, `PointCloudFrame`, and the `StreamMessage::Frame { PointCloudFrame }` envelope round-trip). Old base64-on-wire JpegFrame tests retired with the JSON envelope.

**Companion changes** in [`auki-network-py`](../auki-network-py): `PyDeclineReason` / `PyEndReason` PyO3 wrappers updated for the prost match shapes. Python surface unchanged — `DeclineReason.sensor_not_found()` etc. still work; `kind` and `detail` getters still produce the same snake-case strings.

### broodsugar's dobby · May 8, 07:56 HKT, 2026

[`parking_lot.md`](parking_lot.md) gains three items from an API-surface review walkthrough: (1) **`Capability(pub String)` — open-string vs typed enum** — reader can't tell whether the open-string form is forward-compat-by-design or under-typed; lean toward documenting the open-string-by-design contract with a doc-comment, mirroring the `PEER_DERIVATION_LABEL = "peer/v1"` versioned-string pattern. (2) **`PEER_DERIVATION_LABEL` constant lives in the wrong crate** — the constant's *meaning* belongs in [`auki-identity`](../auki-identity) (it's a `Wallet::derive_child` label); the constant + label semantics should move there, with `auki-network` re-exporting for backward-compat. Cross-references the existing [Wallet → peer-key derivation label evolution](parking_lot.md#wallet--peer-key-derivation-label-evolution) thread. (3) **`StreamDispatch` is the streaming-stability lever — README should call it out** — the closed-enum-by-design rationale is correct (Dagaz Batch 1 #1) but the public README presents it as an implementation aside; suggest adding one sentence to the API-surface table calling out the closed-enum stability model explicitly. Doc-only; no code touch. Surfacing for Nils.

### broodsugar's claude · May 6, 16:30 HKT, 2026

`stream_runtime` lifted to multi-`T` dispatch + `stream_protocol` gains `PointCloudFrame` — [Dagaz](https://www.notion.so/3585c8e96592805b8d83c89f849d3577) Batch 1 (#1 + #2). One PR; cuts as v0.0.20.

**Why this PR is breaking on the producer side and not the consumer side**

grimsby v1 (v0.0.16) pinned `T = JpegFrame` at `ClusterRuntime::spawn` time — the producer's callback was `StreamProvider<JpegFrame>` returning `StreamDecision<JpegFrame>`. Dagaz D1 (RESOLVED 2026-05-06) lifts that pinning to per-call generic on the producer: a single daemon serves multiple `T`s by dispatching on `request.sensor_id`. The consumer side (`open_stream<T>`) was already generic in grimsby v1 — it stays unchanged because each consumer call statically knows the `T` it expects.

**The new producer-side surface**

```rust
pub enum StreamDispatch {
    AcceptJpeg       { info: AcceptInfo, source: SourceStream<JpegFrame>       },
    AcceptPointCloud { info: AcceptInfo, source: SourceStream<PointCloudFrame> },
    Decline          { reason: DeclineReason },
}

pub type StreamProvider = Arc<dyn Fn(StreamRequest) -> StreamDispatch + Send + Sync>;

pub fn decline_all_streams() -> StreamProvider;  // no longer generic
```

**Closed enum, not trait-object.** Adding a new `T` is a deliberate, coordinated SDK + consumer release: bump auki-network, add the variant, every consumer that wants the new sensor type opts in. Trait-object dispatch (open-set) was rejected because Rust generics + serde bounds don't compose well across `dyn Fn` boundaries, and the type-erased version would lose the SDK-level type-checking the consumer surface relies on (per Dagaz D1's rationale: "(B) opaque-bytes rejected — would force the consumer to re-implement decoding the SDK could have type-checked"). New `T` is a 4-line variant addition; cheap.

**Migration for grimsby v1 producers**: `StreamDecision::Accept { info, source }` → `StreamDispatch::AcceptJpeg { info, source }`; `StreamDecision::Decline { reason }` → `StreamDispatch::Decline { reason }`. Decline is variant-stable; Accept becomes typed-by-`T`. The `StreamProvider<JpegFrame>` annotation drops to `StreamProvider`.

**Implementation: non-generic outer + per-`T` typed pump.** `handle_inbound_substream` is now non-generic — reads the request type-free (the `Request` variant of `StreamMessage<T>` carries no `T`-dependent payload, so any concrete `T` deserializes it; we use `JpegFrame` as the marker), invokes the provider, then matches the `StreamDispatch` variant and hands off to a generic `pump_typed::<T>` helper that monomorphizes per `T`. The Decline path writes `StreamMessage::<JpegFrame>::Decline { reason }` — the Decline variant payload is also `T`-free on the wire, so the marker `T` is safe.

**`PointCloudFrame { bytes: Vec<u8> }`** — Dagaz Batch 1 #2, raw-CDR-of-`PointCloud2` per Dagaz D2 (RESOLVED 2026-05-06 — A). The producer forwards CDR bytes verbatim; the consumer (Park) parses CDR on its side. Per-frame layout fields (`row_step`, `height`, `width`, `is_dense`) ride inside the CDR payload itself; sensor-bootstrap fields (`fields[]`, `point_step`, `is_bigendian`, `frame_rate_hz`) live in the producer's `SensorBody::PointCloud` registry entry (boosterapp's `bootstrap_pointcloud`).

**Wire-side of D3** (RESOLVED 2026-05-06): the `bytes` field uses `#[serde(with = "base64_bytes")]` — a small adapter inside `stream_protocol` that serializes `Vec<u8>` as a base64-encoded JSON string instead of `serde_json`'s default array-of-integers encoding. The default would tax the wire ~4× (each byte → ~3.5 ASCII chars + comma); base64 is ~33% overhead vs raw. Bandwidth: 22 MB/s raw `PointCloud2` → ~30 MB/s on the wire (was ~80 MB/s without the adapter). `JpegFrame` keeps the array-of-integers form unchanged — JPEGs are ~1000× smaller, the 4× tax there is tolerable, and changing it would break grimsby v1 wire compat. Producer-side knobs (voxel decimation, reduced cadence) remain for boosterapp at Batch 3 integration.

**New deps** — `base64 = "0.22"` rides on the `swarm` feature (no transitive deps; the `discovery_client` feature already had it). Pure additive; M0 (no features, WASM-friendly) untouched.

**Tests**: 99 unit + 3 integration + 2 doctest with `--all-features` (was 92 + 3 + 2; +7).

`stream_protocol::tests` (+5):
- `point_cloud_frame_serializes_bytes_as_base64_string` — pins `serde_json::to_string(PointCloudFrame { bytes: [0x00, 0x01, 0x02, 0x03, 0xff, 0xfe] })` → `{"bytes":"AAECA//+"}`. Adapter drift fails loudly.
- `point_cloud_frame_round_trips_a_kilobyte_payload` — 1 KB pseudo-random payload round-trips losslessly through serde.
- `point_cloud_frame_wire_size_dodges_the_array_of_integers_tax` — wire-size assertion: 1 KB payload → < 1.5 KB JSON (vs the ~3.4 KB the array-of-integers path would produce). If someone removes the adapter, this fails.
- `locked_point_cloud_frame_wire_shape_vector` — **cross-language conformance vector**. Pins `StreamMessage::Frame { timestamp_ns: 1_700_000_000_000_000_000, seq: 42, payload: PointCloudFrame { bytes: [0x42, 0x43, 0x44, 0x45, 0x00, 0x01, 0xfe, 0xff] } }` → exact JSON `{"kind":"frame","timestamp_ns":1700000000000000000,"seq":42,"payload":{"bytes":"QkNERQAB/v8="}}`. A cross-language reimplementation (Park's browser-side decoder, future Sentinel-as-consumer) is correct only if it produces these exact bytes from the same input. Joins the `auki-hash` / `auki-identity` / `auki-network::tests::locked_seed_to_peer_id_vector` / `auki-identity::tests::locked_sign_canonical_json_vector` cross-language conformance set.
- `point_cloud_frame_round_trips_through_framing_helpers` — full wire-level round trip through `write_message` + `read_message` (length prefix + JSON body).

`stream_runtime::tests` (+2 e2e):
- `producer_accepts_and_streams_pointcloud_frames` — full e2e two-runtime cluster convergence + `open_stream<PointCloudFrame>` + 3 frames + `EndOfStream { SourceEnded }` + iterator-exhausted-after-terminator. Mirrors `producer_accepts_and_streams_jpeg_frames` but with the new dispatch shape.
- `one_producer_serves_jpeg_and_pointcloud_via_sensor_id_dispatch` — one `ClusterRuntime` with a `sensor_id`-keyed multi-`T` provider serves `"camera"` (JPEG) and `"pointcloud"` (PointCloud) over **separate libp2p substreams** multiplexed on the same yamux/QUIC connection. Confirms Dagaz D1's per-call-`T` dispatch end-to-end. Plus declines-on-unknown-sensor — the third substream against `"no-such-sensor"` returns `Err(OpenStreamError::Declined { reason: SensorNotFound })`. This is the core shape boosterapp will use in Batch 3.

Existing JPEG tests all pass with no behavior change — `producer_accepts_and_streams_jpeg_frames`, `producer_declines_unknown_sensor`, `producer_error_signals_consumer_with_detail`, `producer_shutdown_signals_consumer_with_typed_end_of_stream`, `decline_all_streams_returns_sensor_not_found`, `open_stream_against_unreachable_peer_surfaces_typed_error` (now 6 stream_runtime tests; was 6) — confirming the wire bytes for JPEG are byte-identical pre/post-refactor.

**Companion change in `auki-network-py`**: the wrapper's `build_stream_provider` and existing tests updated to construct `StreamDispatch::AcceptJpeg` instead of `StreamDecision::Accept`. **Python surface unchanged** — Python callers still see one `cluster.StreamDecision` PyClass with `.accept` and `.decline` factories that produce JPEG. PointCloudFrame on the Python surface lands in Dagaz Batch 2 (the `auki-network-py` extension for the new `T` shape) — when that lands, `DecisionInner` extends with a `PointCloud` variant and `cluster.PointCloudFrame` becomes a Python type. 33 auki-network-py Rust tests + 39 / 46 Python tests still pass.

Will land in v0.0.20.

---

### broodsugar's claude · May 6, 12:00 HKT, 2026

`discovery_client` module landed — [Vinland](https://www.notion.so/3585c8e9659280699681caec256e0616) Batch 1 piece #2 (Lane B), the SDK-side REST client for `aukilabs/discovery`. Two parallel SDK Claude sessions split Vinland Batch 1 by file ownership: Lane A owns `auki-identity` (`Wallet::sign_canonical_json` + locked vector); Lane B (this crate) owns `auki-network::discovery_client` + the new `discovery_client` Cargo feature. Both pieces ship in v0.0.18.

**Public surface** (gated on the new `discovery_client` feature):

```rust
pub mod discovery_client {
    pub const DEFAULT_TIMEOUT: std::time::Duration = std::time::Duration::from_secs(30);

    pub struct DiscoveryClient { /* base_url + reqwest::Client */ }

    pub enum DiscoveryError {
        Transport(reqwest::Error),
        Status { status: u16, body: String },
        Clock(String),
    }

    impl DiscoveryClient {
        pub fn new(url: impl Into<String>) -> Self;
        pub fn with_http(url: impl Into<String>, http: reqwest::Client) -> Self;
        pub fn base_url(&self) -> &str;

        pub async fn register(
            &self,
            wallet: &auki_identity::Wallet,
            cluster_name: &str,
            addresses: &[multiaddr::Multiaddr],
            expected_app_id: Option<&str>,
            note: Option<&str>,
        ) -> Result<cluster_doc::ClusterDoc, DiscoveryError>;

        pub async fn fetch(
            &self,
            cluster_name: &str,
        ) -> Result<cluster_doc::ClusterDoc, DiscoveryError>;

        pub async fn deregister(
            &self,
            wallet: &auki_identity::Wallet,
            cluster_name: &str,
        ) -> Result<(), DiscoveryError>;
    }
}
```

**Wire shape** (locked end-to-end against Discovery's `verify.rs` / `api.rs` 2026-05-06):

- `POST /clusters/{name}/peers` body: `{ peer_id, public_key (base64-32), addresses, expected_app_id?, note?, timestamp_ns, signature (base64-64) }`. Signed bytes are JCS over the same fields **plus** `cluster_name` (so a captured registration can't be replayed against a different cluster name) and **minus** `signature`. Optional fields are omitted when `None`, both on the wire and in the canonical bytes.
- `GET /clusters/{name}` returns `Json<ClusterDoc>`.
- `DELETE /clusters/{name}/peers/{peer_id}` body: `{ peer_id, public_key, timestamp_ns, signature }`. Signed bytes are JCS over `{ cluster_name, peer_id, op: "delete", timestamp_ns }` — `public_key` rides on the wire body but is **NOT** in the signed bytes (Discovery's `verify_peer_id` already binds pubkey ↔ peer_id, so signing pubkey alongside is redundant). Pinned in `deregister_signature_verifies` because I got this wrong on the first pass and integration-tested my way back to the right shape.

**Identity recipe**: `register`/`deregister` accept the daemon's *parent* `Wallet`; the client internally derives the peer-key wallet via `wallet.derive_child(PEER_DERIVATION_LABEL)` and signs with it. The `peer_id` and `public_key` on the wire are the child's, matching `PeerIdentity::from_wallet`. Same recipe Discovery's verify_peer_id reconstructs.

**Vinland D1, D2, D3 alignment**: `register` is one-shot at startup (D1 — no liveness loop); `fetch` is pull-only on demand (D2 — no SDK-side poll); errors propagate to the caller (D3 — no SDK-side fallback to a local `cluster.json`). Re-register / poll loops deferred to Vinland v2 once Discovery adds TTL or push.

**New `discovery_client` Cargo feature** pulls in `reqwest` (default-features = false; `json` + `rustls-tls`), `base64` (`std`), `auki-jcs` (path-dep), and `thiserror`. Async-only (caller drives a tokio runtime) — no blocking client by intent; daemons running on a tokio runtime call `.await`, ones that don't can wrap with `tokio::runtime::Handle::current().block_on(...)` or `tokio::task::spawn_blocking`. The feature is opt-in (not in `default`) because reqwest + rustls is a fairly heavy dep set that not every consumer needs; the `swarm` feature stays default-off too, mirroring the existing convention.

**Built in parallel against `develop`** using `auki_jcs::canonicalize(&payload) + signing.sign(canonical)` directly so Lane B compiled clean while Lane A's [#44](https://github.com/aukilabs/auki-sdk/pull/44) was still in flight. Once #44 merged earlier today, Lane B picked up `Wallet::sign_canonical_json` in the promised two-line refactor — the production path is now `let (canonical, sig) = signing.sign_canonical_json(&payload)`. Wire bytes are byte-identical (same JCS canonicaliser + same ed25519 sign, just behind a tidier surface). All 9 unit tests + 2 integration tests pass before AND after the refactor with zero changes to Discovery's verifier; the locked cross-language conformance vector binds the same 64-byte signature to the same canonical bytes regardless of which side of the refactor produced them. The internal `register_payload_value` / `deregister_payload_value` helpers return `serde_json::Value` (the input shape `sign_canonical_json` expects); tests reproduce the canonical bytes via `auki_jcs::canonicalize(&register_payload_value(...))` for tampering / replay assertions.

**Tests**: 9 new unit tests + 2 new integration tests (ignored by default; opt-in via `DISCOVERY_BIN=/path/to/discovery cargo test --features discovery_client -- --ignored`).

Unit: `register_signature_verifies_under_wire_pubkey` (rule 4 of Discovery's verify order); `register_pubkey_reconstructs_peer_id` (rule 2); `tampered_addresses_break_signature` (`addresses` is in the signed bytes); `cross_cluster_replay_breaks_signature` (`cluster_name` is in the signed bytes — cluster A signature doesn't verify under cluster B's canonical bytes); `optional_fields_omitted_when_none` (parses canonical JSON, asserts no `expected_app_id`/`note` keys); `deregister_signature_verifies` (matches Discovery's `{cluster_name, peer_id, op, timestamp_ns}` shape and asserts `public_key` is on the wire body but **NOT** in the signed bytes); `locked_register_canonical_and_signature_vector` (cross-language vector — pins parent seed `[3u8; 32]` + fixed cluster + fixed addresses + fixed timestamp `1_700_000_000_000_000_000` to exact RFC 8785 canonical bytes + exact 64-byte ed25519 signature; pairs with `auki-identity::tests::locked_derive_child_peer_v1_pubkey_vector` and `auki-network::tests::locked_seed_to_peer_id_vector`); `with_http_uses_supplied_client_and_url` + `new_trims_trailing_slash` (URL hygiene).

Integration (`tests/discovery_integration.rs`, `#[ignore]`): `discovery_round_trip` boots a real Discovery on a fresh tempdir + ephemeral loopback port, registers Sentinel + Booster into "vinland", fetches both, deregisters Sentinel, asserts Booster remains, asserts second sentinel deregister returns 404 (idempotent on Discovery's side), asserts unknown cluster fetch returns 404. `discovery_rejects_invalid_cluster_name` posts a path-traversal `cluster_name` (`"../etc/passwd"`) and asserts Discovery rejects it. The fixture spawns Discovery as a child process under `Drop`-guarded teardown so a panicking test doesn't leak a daemon.

**Test counts**: no-features unchanged at 36 unit + 3 integration + 1 doctest (the new tests are gated on the new feature). `--features discovery_client` alone runs 45 unit + 3 integration + 1 doctest (baseline 36 + 9 new in `discovery_client::tests`). `--all-features` runs 92 unit + 3 integration + 2 doctest (was 83 + 3 + 2; +9 from discovery_client). `--features swarm` and `--features app_instance` paths unchanged at 74 + 3 + 2 / 45 + 3 + 0 respectively. The 2 integration tests in `tests/discovery_integration.rs` are `#[ignore]`-marked + gated on `DISCOVERY_BIN` so they don't run in the default integration suite count.

**No breaking changes** — purely additive on a new feature flag.

Closes Vinland Batch 1 alongside Lane A's [#44](https://github.com/aukilabs/auki-sdk/pull/44); cuts as v0.0.18.

---

### broodsugar's claude · May 5, 19:00 HKT, 2026

`stream_runtime`: new test `open_stream_against_unreachable_peer_surfaces_typed_error` (6th `stream_runtime` test) — closes the second of v0.0.16's documented follow-ups. Pins the existing behavior of `ClusterRuntime::open_stream` against a peer in `cluster.json` whose address refuses connections (port 1 — reserved, RST'd immediately by Linux/macOS): the call returns `Err(OpenStreamError::LibP2p(_))` or `Err(OpenStreamError::Timeout(_))` within the 30s `OPEN_STREAM_TIMEOUT` bound, doesn't hang, doesn't panic. Wraps an outer 35s `tokio::time::timeout` as a safety net to fail the test rather than hang if the SDK-side bound ever breaks. Both variants are explicit "request failed" signals the consumer's app-level reconnect logic can act on, so the test admits either; in practice libp2p RST-fast-fails inside ~80ms on the LAN-loopback case. No behavior change — just locks in what was already shipping. Test counts: 74 unit + 3 integration + 2 doctest with `--features swarm` (was 73 + 3 + 2); 83 unit with `--all-features` (was 82). Will land in v0.0.17 alongside the producer-shutdown follow-up below.

### broodsugar's claude · May 5, 18:30 HKT, 2026

`ClusterRuntime::shutdown` now flushes a typed `EndOfStream { reason: ProducerShuttingDown }` to every in-flight inbound stream substream before tearing the swarm down — closing the [grimsby](https://www.notion.so/3575c8e965928079a955ed9573bbb398) D5b "best-effort explicit" gap that v0.0.16 had left as a follow-up. New private `inbound_shutdown_tx: watch::Sender<bool>` field on `ClusterRuntime`; `from_swarm` initialises it with `false` and threads a clone through to each spawned `handle_inbound_substream` task. `shutdown(self)` sends `true` first; the `run_task` shutdown-receive arm then sleeps `SHUTDOWN_GRACE = 100ms` before returning, giving the per-substream tasks time to flush their `EndOfStream` while the swarm + connections are still alive. Per-substream pump loop now races `source.next()` against `shutdown_rx.changed()` in a `tokio::select! { biased; ... }`; on shutdown it breaks out with `EndReason::ProducerShuttingDown` and lets the existing post-loop `EndOfStream` write fire. **Drop path unchanged** — `Drop` (unclean exit, panic, forgot to call `shutdown(self)`) skips the explicit signal and the consumer falls back to `ConnectionLost`. That's the documented fallback shape; explicit `shutdown(self)` is the API for clean producer exit. **Sender-dropped semantics**: if the runtime exits via `Drop` rather than `shutdown(self)`, the watch sender drops and `shutdown_rx.changed()` returns `Err`. The pump-loop branch handles this by `continue`-ing rather than returning an EndOfStream — falls through to the implicit ConnectionLost on the next failed write. New regular dep: `tokio` `sync` feature was already on; `watch` channel is part of `sync`. New test `producer_shutdown_signals_consumer_with_typed_end_of_stream` (the 5th `stream_runtime` test): producer source-Stream is `iter([first_frame]).chain(pending())`; consumer reads first frame; `producer.shutdown()` is called; consumer's iterator next item is asserted as `Err(EndOfStream { reason: ProducerShuttingDown })` (not `ConnectionLost`); iterator exhausted after. 5s timeout on the read to absorb any libp2p propagation jitter — typical convergence is well under SHUTDOWN_GRACE + RTT. Test counts: 73 unit + 3 integration + 2 doctest with `--features swarm` (was 72 + 3 + 2); 82 unit with `--all-features` (was 81); no-features and `--features app_instance` paths unchanged. No breaking API changes — `shutdown(self)` signature is unchanged; behavior strictly improves (was: `ConnectionLost`; now: typed `ProducerShuttingDown`). Will land in v0.0.17, likely alongside Lane B's `auki-network-py` stream extensions.

### broodsugar's claude · May 5, 17:30 HKT, 2026

`Stream<T>` Rust API + `ClusterRuntime` stream extension landed — [grimsby](https://www.notion.so/3575c8e965928079a955ed9573bbb398) deliverables #2 + #3, the typed Rust API on top of the wire primitives in #1. Two-side surface: producer (`StreamProvider<T>` callable returning `StreamDecision<T>`, app-supplied source-Stream of `ProducerFrame<T>` items the SDK pumps onto the wire) + consumer (`ClusterRuntime::open_stream<T>(peer_id, request)` returning a `StreamSubscription<T>` whose `frames` iterator yields `Result<ConsumerFrame<T>, StreamError>` items, terminator-then-`None` semantics). New `auki_network::stream_runtime` module (gated on `swarm`); existing `cluster_runtime::ClusterRuntime` extended with a `stream_provider: StreamProvider<JpegFrame>` arg on `spawn` / `from_swarm` (**breaking** — every existing caller needs to thread one through; the tests use the new `decline_all_streams()` convenience for consumer-only shape) plus the new `open_stream` async method. The runtime task gains an `IncomingStreams` arm in its `tokio::select!`; inbound substreams from peers in `cluster.json` are handed to per-substream `handle_inbound_substream` tasks; outsiders dropped silently same as `cluster_protocol`'s trust boundary. **One swarm, one driver task** per grimsby's "share the swarm" decision — no second libp2p stack, no separate Noise handshake. **Producer-side ****`T`**** locked to ****`JpegFrame`**** at runtime construction time** per grimsby D4; `open_stream<T>` is generic on the consumer side. **`StreamError`** variants: `EndOfStream { reason: EndReason }` (graceful end, producer-supplied reason), `ConnectionLost` (substream closed without explicit EndOfStream), `Protocol(StreamProtocolError)` (malformed bytes). **`OpenStreamError`** variants: `Declined { reason: DeclineReason }`, `LibP2p(libp2p_stream::OpenStreamError)`, `Protocol(StreamProtocolError)`, `Timeout(Duration)`. **`OPEN_STREAM_TIMEOUT = 30s`** for the libp2p substream open + handshake round trip. **Producer error path**: source-Stream yields `Some(Err(detail))` → SDK writes `EndOfStream { reason: ProducerError { detail } }` and closes; source returns `None` → `EndOfStream { reason: SourceEnded }`. **Consumer disconnect**: SDK reader task notices its mpsc `Sender` failed, exits, drops substream, libp2p closes — producer's source-Stream gets dropped on the next pump cycle, app's `Drop` releases hardware/buffer resources cleanly. **`seq` auto-stamping**: SDK assigns `seq = 0, 1, 2, ...` per substream; producers don't track it. **Pattern A locked** for the Python binding (Lane B / `auki-network-py`) — SDK owns the Python event loop internally, matching `cluster_runtime::participant_provider`'s sync-callable precedent; sidecar consumers stay sync-shaped. Documented in the grimsby Notion doc's deliverable #2 surface section. 4 new tests: `producer_accepts_and_streams_jpeg_frames` (e2e happy path: two-runtime converge → open → 3 frames + `SourceEnded` → iterator exhausted), `producer_declines_unknown_sensor` (decline path: typed `Err(Declined { reason: SensorNotFound })`), `producer_error_signals_consumer_with_detail` (error path: source yields `Some(Err("encoder died"))` → consumer sees `Err(EndOfStream { reason: ProducerError { detail: "encoder died" } })`), `decline_all_streams_returns_sensor_not_found` (helper unit test). 11 existing `cluster_runtime` tests updated for the new signature; all passing. Test counts: 72 unit + 3 integration + 2 doctest with `--features swarm` (was 68 + 3 + 2); 81 unit with `--all-features` (was 77); no-features and `--features app_instance` paths unchanged at 36 + 3 + 1 / 45 + 3 + 1. **No Lane B sign-off was waited on** — Nils explicitly overrode the "wait for sign-off" framing with "lets continue shipping" once Arshak surfaced as blocked needing the protocol; the surface posted in the Notion doc earlier is now what was implemented; if Lane B's PyO3 bridge surfaces friction, Lane A will adjust the Rust surface in a follow-up. Outer README + `src/readme.md` updated for the new module + the new methods on `ClusterRuntime`. **Pushed as commit `48e2aea` on PR [#39](https://github.com/aukilabs/auki-sdk/pull/39)** alongside #1; the PR now contains all of grimsby Batch 1.

### broodsugar's claude · May 5, 16:00 HKT, 2026

`/auki/stream/0.1.0` typed-byte-stream protocol primitives landed — [grimsby](https://www.notion.so/3575c8e965928079a955ed9573bbb398) deliverable #1, the wire half of grimsby's "Park subscribes to live RGB from a Booster over libp2p instead of polling `/api/preview/latest.jpg` over HTTP" plan. New `auki_network::stream_protocol` module (gated on `swarm`) ships the wire types, framing helpers, and `libp2p_stream::Behaviour` wiring; the `Stream<T>` Rust API + `stream_provider` runtime are separate deliverables (#2, #3) that build on these primitives. **Wire types**: `StreamRequest { sensor_id }`, `StreamMessage<T> = Request | Accept | Decline | Frame | EndOfStream` (tagged `kind` on JSON, generic over payload `T`), `AcceptInfo { sensor_hash, clock_id, clock_hash }`, `DeclineReason = SensorNotFound | SensorUnavailable | ProducerShuttingDown | Other { detail }`, `EndReason = SourceEnded | ProducerShuttingDown | SessionEnded | ProducerError { detail }`, `JpegFrame { bytes: Vec<u8> }` (the `T` for grimsby v1, byte-identical to what `/api/preview/latest.jpg` serves today over HTTP). **Framing**: 4-byte big-endian u32 length prefix + JSON-serialized `StreamMessage<T>`; cap `MAX_FRAME_BYTES = 16 MiB` so a peer can't drive an OOM by claiming a huge length. **Helpers**: `write_message<T: Serialize, S: AsyncWrite + Unpin>(stream, msg)` and `read_message<T: DeserializeOwned, S: AsyncRead + Unpin>(stream)` over the `futures` async-IO traits (matches `libp2p_stream`'s `libp2p::Stream`). Errors via `StreamProtocolError = Io | Serialize | Deserialize | FrameTooLarge | EmptyFrame`. **Swarm integration**: new always-on `stream: libp2p_stream::Behaviour` field on the swarm `Behaviour` struct (sibling to `cluster: cluster_protocol::Behaviour`); receivers acquire a `Control` via `behaviour().stream.new_control()` and bind to `STREAM_PROTOCOL = "/auki/stream/0.1.0"` via `Control::accept`. **Decisions** (D1–D5 walked through with Nils 2026-05-05, all resolved): D1 single bidirectional substream (one protocol, substream lifetime = subscription lifetime); D2 sensor_id-only addressing for v1 (Layer 2 capability advertisement deferred); D3 provider returns a `futures::Stream` (SDK owns the pump task; backpressure flows naturally substream→source); D4 `T = JpegFrame` for v1 (other `T`s — `SensorLogEntry`, `PointCloudLogEntry`, `PoseLogEntry` — are sibling instantiations later); D5 SDK is mechanism-only on lifecycle (slow-consumer shedding is the producer's source-Stream's concern; consumer re-requests on stream death). New regular dep gated on `swarm`: `libp2p-stream = "=0.4.0-alpha"` — the libp2p umbrella crate doesn't expose `stream` as a feature flag in 0.56, so this ships as a separate sibling crate; pre-1.0, pinned exactly until the upstream surface stabilizes. 13 new tests (protocol id locked, max-frame-bytes locked, all 5 message variants round-trip through serde-JSON, full `Request → Accept → Frame×3 → EndOfStream` session round-trips through the framing helpers in order, oversize length-prefix rejected before allocation, empty length-prefix rejected, EOF surfaced as `Io(UnexpectedEof)`, oversize payload rejected before I/O write). Test counts: 68 unit + 3 integration + 2 doctest with `--features swarm` (was 55 + 3 + 2); 77 unit with `--all-features` (was 64); no-features and `--features app_instance` paths unchanged. Outer README + `src/readme.md` updated with the new module's surface and the stream-protocol example. **No breaking changes** — purely additive on the `swarm` feature path. Grimsby implementation continues with deliverable #2 (`Stream<T>` Rust API; surface to be posted in the grimsby Notion doc for Lane B / `auki-network-py` sign-off before lock).

### broodsugar's claude · May 5, 13:00 HKT, 2026

Docstring clarification on `ClusterRuntime::spawn` and `::from_swarm`: the `seed` argument is the **peer** seed (post-wallet-derivation), not the wallet seed. Surfaced 2026-05-05 by BoosterApp Claude when wiring deliverable #7 — `spawn` calls `PeerIdentity::from_seed(&seed)` (direct ed25519, not `from_wallet`), so passing a wallet seed produces a swarm whose PeerId differs from `wallet.derive_child("peer/v1").peer_id()` (the canonical wallet-rooted peer identity that operators put in `cluster.json`); libp2p Noise rejects every dial with no clear error message. The previous docstring said "32-byte ed25519 seed (typically from `Wallet::derive_child(\"peer/v1\").seed()`)" in the README example but didn't flag the wallet-vs-peer distinction in the API docs themselves. New docstring on `spawn` walks through the wallet-rooted invocation pattern inline (`Wallet::derive_child(PEER_DERIVATION_LABEL)` → `peer_wallet.seed()`) and points to `auki-identity-py`'s sibling `Wallet::seed` for the Python angle; `from_swarm`'s docstring picks up a one-liner reminder that the swarm's keypair carries the same caveat. No behavior change. Test counts unchanged (55 unit + 3 integration + 2 doctest with `--features swarm`). Will land in v0.0.15 alongside the `Wallet.seed` PyO3 binding addition in `auki-identity-py`.

### broodsugar's claude · May 5, 10:30 HKT, 2026

`ClusterRuntime` `participant_provider` signature changed from `Arc<dyn Fn() -> ParticipantInfo + Send + Sync>` to `Arc<dyn Fn() -> Option<ParticipantInfo> + Send + Sync>` — small follow-up promised to Booster claude after #34 landed. Returning `None` tells the runtime to drop the inbound request's reply channel without sending a response; the requester sees a request timeout, which is the correct signal when the consumer can't fill in valid info (session clock not yet bound during sidecar startup, Python `participant_provider` raised an exception that the PyO3 wrapper caught and logged, or any other transient inability to construct a valid `ParticipantInfo`). The runtime is unaffected — it stays alive, continues processing other events, and accepts future requests from the same peer. Inside the request handler in `run_task`, the existing `let info = (participant_provider)(); send_response(channel, info)` becomes `if let Some(info) = (participant_provider)() { send_response(channel, info) }` plus a comment distinguishing the None-from-the-provider case (drop) from the peer-not-in-doc case (also drop, but for a different reason — the cluster doc is the trust boundary, not the responder's transient state). Existing fixture closures wrap their static `ParticipantInfo` in `Some(...)`. **Breaking API change** for any out-of-tree consumer holding a `ParticipantInfoProvider` directly — but the only consumer today is internal tests; `auki-network-py` (in flight on the BoosterApp side) hasn't shipped yet, so the breaking change has zero external impact. New test `cluster_runtime::tests::provider_returning_none_drops_the_reply` (the eighth `cluster_runtime` test) sets up two runtimes: rt_a's provider always returns `None`, rt_b's is a normal fixture; asserts the asymmetric peer view (rt_a sees rt_b because rt_b replies normally, rt_b does NOT see rt_a because rt_a drops the channel), then verifies both runtimes still respond to `peers()` and `shutdown()` calls without panic or hang. 55 unit + 3 integration + 2 doctest with `--features swarm` (was 54 + 3 + 2); 64 unit with `--all-features` (was 63); no-features and `--features app_instance` paths unchanged. Module-level docstring on `ParticipantInfoProvider` documents the `None` semantics inline. Outer README's "Cluster runtime (M1)" table row for `participant_provider` updated; `src/README`'s mod-block sketch updated; tests table gains the new row. No new parking-lot items. Will land in v0.0.13 alongside ansuz Batch 2 and the in-flight `auki-network-py`.

### broodsugar's claude · May 5, 09:30 HKT, 2026

`ClusterRuntime` landed — ansuz networking-demo deliverable #4, the higher-level cluster orchestration on top of the `cluster_protocol` primitive (#3). New `auki_network::cluster_runtime` module (gated on `swarm`) wraps a libp2p `Swarm<Behaviour>` + tokio task in an opaque shape: `ClusterRuntime::spawn(seed, doc, swarm_config, participant_provider)` builds the swarm and starts the driver; `ClusterRuntime::from_swarm(swarm, doc, participant_provider)` accepts a pre-built swarm (the test-and-tooling seam — lets a caller learn the swarm's bound addresses before constructing the cluster doc); `peers() -> Vec<PeerSnapshot>` returns the live view; `shutdown(self)` is the explicit clean exit; `Drop` is the safety net. Both lifecycle paths abort the driver task and close the swarm. Driver behaviour: auto-dials every peer in `cluster.json` that has at least one address; on `ConnectionEstablished` to a known peer, sends a `ClusterRequest`; on inbound request from a known peer, invokes `participant_provider` and replies; on inbound request from an unknown peer, drops the channel (their request times out — the cluster doc IS the trust boundary, period); on response from a known peer, stores in the state map keyed by `PeerId`; on `ConnectionClosed`, marks the entry disconnected (preserving `first_seen_ns` for same-session reconnect) and schedules a redial; on `OutgoingConnectionError`, schedules a redial. Reconnect is per-peer exponential backoff, `INITIAL_BACKOFF = 1s` doubling up to `MAX_BACKOFF = 60s`, reset on a successful connect. The driver's `tokio::select!` loop arms a `RECONNECT_TICK = 500ms` interval to walk the schedule and fire due dials. **`first_seen_ns` semantics**: the peer's `session_now_ns` value at the moment of the **first** response received from their current session — sticky across reconnects within the same peer-session, reset if the peer's `session_id` changes (peer restarted with a fresh session). The store-response logic compares incoming `session_id` against the existing entry's; same-session refresh keeps `first_seen_ns`, different-session replace resets it. **`cluster_joined_at_ns` setter is the consumer's job** — the runtime explicitly does not mutate the local `ParticipantInfo`; the consumer reads `peers()` and sets the field on its own outbound info accordingly. The runtime's `participant_provider` callable is invoked **per inbound request** (not once at spawn time) so `session_now_ns` is fresh on each reply rather than stale at runtime-spawn — this is what lets `auki-py`'s Python `participant_provider` plug in cleanly. **Shape decision** (recorded in the ansuz Notion doc 2026-05-05): opaque runtime owning its own swarm + task, *not* a real `NetworkBehaviour`. The wrapper consumers are Boosterapp via `auki-py` (Python can't drive a libp2p loop, wants opaque) and Sentinel (Rust, but uses `cluster_protocol::Behaviour` directly and drives its own loop); neither wants `NetworkBehaviour` composition, and the runtime path is meaningfully simpler to write and test. A future Rust consumer that wants `NetworkBehaviour` composition can build it on top of `cluster_protocol` directly. New regular deps gated on `swarm`: `tokio` (`macros` + `rt` + `sync` + `time`) for the driver task primitives; `futures` (default-features off) for `StreamExt::next` on the swarm. Both were already dev-deps for the existing swarm tests; promoting to regular deps adds nothing to the dep graph that `libp2p` 0.56's `tokio` feature didn't already pull in. 7 new tests, all `#[tokio::test(flavor = "multi_thread")]`: `two_runtimes_discover_each_other_via_cluster_doc` (the basic happy path — both sides see the other in `peers()` within 10s), `three_runtimes_form_full_mesh` (each runtime sees the other two — within 15s), `peer_leaving_drops_off_other_peers` (3 runtimes converge, kill one, surviving 2 drop the departed peer from `peers()` while keeping each other), `unknown_peer_is_not_surfaced` (a swarm not in the doc dials in and sends a request — the runtime drops it silently, `peers().len() == 0`), `shutdown_is_idempotent_and_drops_state` and `drop_without_explicit_shutdown_cleans_up` (lifecycle paths return promptly, no deadlock), `spawn_outside_tokio_runtime_returns_error` (calling `from_swarm` from a `std::thread` → `SpawnError::NoTokioRuntime`). Test counts: 54 unit + 3 integration + 2 doctest with `--features swarm` (was 47 + 3 + 2); 63 unit with `--all-features` (was 56); no-features and `--features app_instance` paths unchanged. **Closes ansuz Batch 2.** No new parking-lot items raised. Boosterapp's `auki-py` PyO3 wrapper (`cluster.spawn` etc.) is unblocked.

### broodsugar's claude · May 5, 09:00 HKT, 2026

`/auki/cluster/0.0.1` participant-exchange protocol landed — ansuz networking-demo deliverable #3, the libp2p half of the **one schema, two transports** promise. New `auki_network::cluster_protocol` module (gated on the `swarm` feature) exposes the protocol id constant `CLUSTER_PROTOCOL = "/auki/cluster/0.0.1"`, a per-request timeout `REQUEST_TIMEOUT = 30s`, the `ClusterRequest` unit struct (serializes as JSON `null` — empty by design, since the protocol's only purpose is fetching the responder's `ParticipantInfo`), the `ClusterResponse = ParticipantInfo` type alias, a `Behaviour = libp2p::request_response::json::Behaviour<ClusterRequest, ClusterResponse>` type alias, and a `behaviour()` constructor that wires both `ProtocolSupport::Full` (every peer can both ask and answer) and the 30s timeout. Codec is libp2p's JSON convenience — same wire bytes the existing `participant::golden_bytes_match_fixture` test pins for `GET /api/info`, so a Park that consumes the HTTP endpoint and a peer that consumes the libp2p response parse identical structs. Wired into the swarm `Behaviour` struct as `cluster: cluster_protocol::Behaviour` — **always-on** (no `Toggle`), the protocol sits idle for swarms that don't participate in a cluster (the dedicated `aukilabs/relay` infrastructure node), so a knob would have been ceremony. The behaviour does **not** auto-respond: receivers handle `request_response::Event::Message::Request{ channel, .. }` themselves and call `behaviour.cluster.send_response(channel, info)` to reply, which is the standard libp2p pattern and lets `auki-py`'s `participant_provider` callable plug in (so `session_now_ns` is fresh on each reply rather than stale at swarm-spawn time). Higher-level orchestration — auto-dialing peers from `cluster.json`, tracking `Joined`/`Left`, holding a peer state map — lives in the next deliverable (cluster runtime / #4); Rust consumers that want fine control (Sentinel) drive the swarm event loop themselves and call `send_request` / `send_response` directly. 3 new tests: `protocol_id_is_locked` (wire-format pin on the protocol id string, same shape as `IDENTIFY_PROTOCOL`'s lock), `request_serializes_as_json_null` (empty unit struct round-trips as JSON `null`), and `two_peers_exchange_participant_info_over_tcp` (full end-to-end: build two swarms, dial, A sends `ClusterRequest`, B receives, B sends back its `ParticipantInfo`, A asserts received == fixture). New libp2p features enabled: `request-response` + `json` (the latter pulls in `serde_json` which we already had on the M0 path). Test counts: 47 unit + 3 integration + 2 doctest with `--features swarm` (was 44 + 3 + 1); no-features and `--features app_instance` paths unchanged at 27 + 3 + 1 / 36 + 3 + 1. No new parking-lot items — multi-cluster participation is single-valued today (daemon's session info is per-session, not per-cluster), and decline-vs-fail isn't load-bearing for ansuz (cluster doc is the trust boundary; libp2p Noise enforces peer-id matching at connection time).

### broodsugar's claude · May 4, 17:30 HKT, 2026

Locked cross-language conformance vector for `PeerIdentity::from_wallet(&Wallet::from_seed(&[3u8; 32])).peer_id()` → canonical libp2p PeerId `12D3KooWAvnEo4RaYZtqt2w83qzmQ7WVW2HhN2cay95EXAiVKcar`. New `tests::locked_seed_to_peer_id_vector` pins the string; if the chain (ed25519 keypair from seed → libp2p `PublicKey` protobuf encoding → multihash → multibase-base58 PeerId) drifts, every reimpl in another language and every `cluster.json` in the wild drifts with it. Pairs with `auki-identity`'s new `locked_derive_child_peer_v1_pubkey_vector` — the two together pin the `Wallet → libp2p PeerId` chain that `cluster.json` depends on. Pattern matches `auki-hash`'s existing locked vectors. Surfaced via a new "Cross-language conformance vectors" section in the root [`README.md`](../../README.md). 1 new test. Cheap insurance ahead of v0.0.11.

### broodsugar's claude · May 4, 16:30 HKT, 2026

`app_instance::derive()` landed — implementation of ansuz #5, the per-machine identifier that distinguishes two daemons of the same `app` running on different hardware. New `auki_network::app_instance` module behind a default-off `app_instance` feature (non-WASM by design — depends on the `mac_address` crate, which uses platform syscalls `getifaddrs` / `GetAdaptersAddresses`). Recipe locked per ansuz D4: enumerate interfaces, skip loopback (all-zero MAC) and locally-administered MACs (U/L bit `0x02` on the first octet), sort the remaining MACs lexicographically by raw bytes, render the smallest as 12 lowercase hex chars without separators (`aabbccddeeff`). Sorting (rather than relying on OS interface enumeration order) gives a stable selection across platforms and reboots for a fixed hardware set. `DeriveError` enum: `NoNetworkInterfaces` (empty enumeration), `NoSuitableMac` (everything filtered — typical in containers), `Io(std::io::Error)` (syscall failure). Public API: `derive() -> Result<String, DeriveError>` for production; `derive_from(macs: &[[u8; 6]]) -> Result<String, DeriveError>` as the testing seam (pure, no side effects). 9 new tests covering the locked `[0x00,0x16,0x3e,0xab,0xcd,0xef]` → `"00163eabcdef"` cross-language vector, schema (12 lowercase hex), `NoNetworkInterfaces` on empty, `NoSuitableMac` on loopback-only / locally-administered-only, IEEE selection skipping each filtered category, lexicographic-first ordering across multiple IEEE MACs, and the U/L-bit logic in isolation. The value this function returns is what PR #25's `/api/info` redesign will carry in its new `app_instance` field; the two PRs are independent. Stability caveats documented (containers, VMs, multi-NIC, MAC randomization) — fragile in those environments by ansuz acceptance; a stable-id alternative (wallet-derived, persisted) is parked for after ansuz. New parking-lot items: container/Docker handling, multi-NIC tiebreaker semantics, eventual stable-id options.
### broodsugar's claude · May 4, 16:00 HKT, 2026

`ParticipantInfo` landed (ansuz networking-demo deliverable #2b) — the wire shape every Auki participant exchanges to introduce itself. New `auki_network::participant` module exposes `ParticipantInfo { app, name, session_id, session_clock_id, session_clock_hash, session_now_ns, cluster_joined_at_ns: Option<u64>, peer_id: PeerId, app_instance }`, re-exported at the crate root. **One schema, two transports**: `GET /api/info` (HTTP, redesigned in [PR #25](https://github.com/aukilabs/auki-sdk/pull/25)) and the `/auki/cluster/0.0.1` libp2p participant protocol (ansuz deliverable #3, separate work) carry exactly the same JSON. Serde uses snake-case field names directly (the struct's Rust names already match the spec'd JSON keys); `cluster_joined_at_ns` serializes as explicit `null` when `None`; `peer_id` rides through `libp2p-identity`'s `serde` feature as the canonical multibase-base58 string (`12D3KooW…`). Lives on the M0 path (no `swarm` feature required) so Park / Console can construct and parse it without pulling in libp2p's transport stack — `libp2p-identity` is already in the default-features set. 8 new tests: round-trip with `Some` and with `None`, snake-case key verification, locked golden-bytes (fixture serializes to exactly the spec'd JSON, with the peer-id derived from a fixed ed25519 seed for reproducibility), explicit-rejection of missing field / wrong type / invalid peer-id string, and a pin on the explicit-`null` vs field-omission distinction. No new dependencies. No parking-lot items raised — the `peer_id` JSON shape (string vs bytes) is settled by both libp2p convention and the spec'd `/api/info` example. Implements ansuz milestone-plan deliverable #2b; the Notion plan is at https://www.notion.so/3565c8e96592809fb674f769d826c1de (D1–D6 all RESOLVED, did not re-resolve).

### broodsugar's claude · May 4, 14:30 HKT, 2026

`cluster.json` discovery-doc spec + loader landed (ansuz milestone deliverable #1). New always-on `cluster_doc` module sits alongside the M0 data types — no feature gate, native-only (uses `std::fs`); the rest of the crate's M0 path stays WASM-friendly because Console doesn't import this module. `ClusterDoc` carries `{version: u32, cluster_name: String, peers: Vec<ClusterPeer>}`; each `ClusterPeer` is `{peer_id: libp2p_identity::PeerId (required), addresses: Vec<multiaddr::Multiaddr>, expected_app_id: Option<String>, note: Option<String>}` — `peer_id` required per ansuz D1 (strict, libp2p Noise rejects mismatches at connection time so the doc gives identity continuity across daemon restarts), `expected_app_id` advisory only (authoritative `app_id` rides over the wire from `/api/info`). `LoadError` enum: `Io`, `Parse`, `UnsupportedVersion(u32)`, `InvalidPeerId(String)`, `InvalidMultiaddr(String)` — typed-field errors carry the offending value so an operator can fix the doc from the message alone; manual `Display` + `Error` impls (no `thiserror` dep on the M0 path). `load(&Path) -> Result<ClusterDoc, LoadError>` does a two-phase parse: peeks at `version` first so a future v2 doc with a restructured shape still surfaces `UnsupportedVersion` cleanly instead of an opaque `Parse`. Path layout: `<app_root>/registries/cluster_registries/cluster.json` — flat (single file, not hash-keyed like the existing `sensors/` / `clocks/` / `frames/` siblings); `default_path(app_root)` returns the canonical join, `resolve_path(app_root, cli_override)` honours `cli_override > $AUKI_CLUSTER_DOC > default` (empty env value treated as unset; the override wins over an env var when both are set). 16 new unit tests covering the full schema + every `LoadError` variant + path-resolution precedence + an empty-env-as-unset edge case (env-mutating tests serialize on a process-wide `OnceLock<Mutex>` to keep them parallel-safe alongside the rest of the suite). 3 integration tests in `tests/cluster_doc.rs` exercise the daemon-startup flow end-to-end: `resolve_path` → on-disk JSON → `load` at the default layout, the same with a CLI override, and the operator-typo path returning `InvalidPeerId` with the offending value. New `tempfile` dev-dep. Test counts now 35 unit + 3 integration + 1 doctest with `--features swarm` (was 19 + 0 + 1); 27 unit + 3 integration without features (was 11 + 0 + 0). M0 build with no features still clean. Empty-`addresses` peers are explicitly allowed at parse time (operator may temporarily de-route a pinned peer); consumers that need a dialable address should filter such entries out. `cluster_registries/` is intentionally **flat** for ansuz — lifting the cluster doc into a Cluster Registry primitive (hash-keyed, signed, etc.) is a future evolution, parking-lot item added. The doc is also unsigned for ansuz; cryptographic attestation of the cluster membership list is a future concern. Implements ansuz D1 (strict — peer_id required); D2/D3/D4/D5/D6 unaffected by this PR.

### broodsugar's claude · May 2, 18:45 HKT, 2026

M1b landed — Circuit Relay v2 + libp2p mDNS, plus a `dial_peer` helper for Park-from-home circuit-relay dialing. Encodes the three resolved Reid milestone-2 parking-lot answers (1a / 2c / 3c, all decided 2026-05-02). New libp2p features enabled: `mdns`, `relay`. `Behaviour` extended with `Toggle<mdns::tokio::Behaviour>`, `relay::client::Behaviour` (always wired into the transport stack via `with_relay_client`), and `Toggle<relay::Behaviour>`. `SwarmConfig` gains `enable_mdns: bool` (default true — daemons advertise on `_p2p._udp.local.` alongside their existing `_auki._tcp.local.` advertisement; tests opt out to avoid LAN noise) and `enable_relay_server: bool` (default false — consumer daemons don't relay; the dedicated `aukilabs/relay` app sets it true). `Capability::*` advertisement in `ReachabilityRecord` is independently gated by consumers — both knobs must line up for the peer to actually serve the capability. New `swarm::dial_peer(&mut Swarm, PeerId, Vec<Multiaddr>)` helper takes direct or circuit-relay multiaddrs (e.g. `/p2p/<relay>/p2p-circuit/p2p/<target>`); Park-from-home is operator-paste of `(peer-id, [optional relay multiaddr])` for the M2 demo. 4 new tests (build-with-mdns, build-with-relay-server, full reservation flow client→relay→ReservationReqAccepted, dial_peer helper); 19 unit tests + 1 doctest total. Reservation test required `add_external_address` on the relay swarm so its loopback listen address gets included in reservation responses (real networks learn external addresses via identify or AutoNAT). All resolved Propagate items removed from `parking_lot.md`; remaining items (peer-derivation label, `ReachabilityRecord` extensibility, `SwarmConfig` knob minimalism, `BuildError::Transport` structure) are forward-looking, not M1-blocking. New parking-lot items added: loopback `add_external_address` workaround note, DCUtR/hole-punching as future work. Layer 2 (capability advertisement / discovery via libp2p `request_response`) is the next chunk.

### broodsugar's claude · May 2, 17:30 HKT, 2026

M1a landed — libp2p `Swarm` builder behind the `swarm` feature. Composes `libp2p::tcp` + `libp2p::quic` transports under `libp2p::noise` (authenticated with the peer's ed25519 keypair) and `libp2p::yamux` multiplexing, with a minimal `identify` + `ping` `NetworkBehaviour`. New `auki_network::swarm` module exposes `build_swarm(&PeerIdentity, SwarmConfig) -> Result<libp2p::Swarm<Behaviour>, BuildError>`; the constructed swarm has its `local_peer_id` matching `identity.peer_id()` and is already listening on every `SwarmConfig.listen_addresses` entry before returning. Identify protocol id is `/auki/identify/0.0.1` (stable wire format); `agent_version` is the per-deployment knob. Idle connections close after 60s (ping resets the timer). 4 new tests + 1 doctest covering `build_swarm` correctness, mutual `identify::Event::Received` round-trip over TCP, the same over QUIC, and listening on multiple addresses simultaneously. `swarm` is **default-off** so the M0 path stays WASM-compilable for Console (which uses `default-features = false`); enabling the feature pulls in `libp2p` 0.56 (features: `tokio`, `tcp`, `quic`, `noise`, `yamux`, `identify`, `ping`, `macros`, `ed25519`) and `thiserror`. Workspace test count: 11 → 15 unit + 1 doctest. Next: M1b adds Circuit Relay v2 (`libp2p::relay` client always, server wrapped in `Toggle` for consumer-daemon off-by-default) and `_p2p._udp.local.` mDNS coexistence — both blocked on the parking-lot questions resolving first.

### broodsugar's claude · May 2, 16:10 HKT, 2026

Crate created — Layer 1 of the Reid milestone-2 networking stack. Ships the data types only: `PeerIdentity` (libp2p ed25519 keypair derived from a wallet via `derive_child("peer/v1")`, with `peer_id()` / `public_key()` / `keypair()` accessors), `ReachabilityRecord` (peer id + multiaddrs + capabilities + last-seen, JSON-serializable wire shape for peer discovery), and `Capability` (namespaced-string newtype with the four canonical `networking:*` constants from the Reid milestone-2 architecture: `MESSAGE_FORWARDING`, `BULK_DATA_CHANNEL`, `TURN`, `SFU`). 11 tests covering determinism of derivation, the public `from_wallet ≡ from_seed(derive_child("peer/v1").seed())` contract, JSON round-trips, and capability namespace extraction. WASM-friendly (`libp2p-identity` + `multiaddr` both compile to WASM); deliberately split from M1's libp2p `Swarm` so Console can derive a peer id from an in-browser wallet without pulling in the transport stack. Built on `auki-identity`, `libp2p-identity` 0.2 (`ed25519` + `peerid` + `serde` features, `default-features = false`), and `multiaddr` 0.18 (with a small local serde adapter — `multiaddr` 0.18 dropped its serde feature). M1 will add the libp2p `Swarm` (TCP/QUIC + Noise + Yamux + Circuit Relay v2) on top of these primitives. Parking-lot items: mDNS coexistence (`_p2p._udp.local.` vs `_auki._tcp.local.`); Wallet→peer-key derivation label evolution; Park-from-home access pattern; off-by-default relay-server plumbing; `ReachabilityRecord` extensibility / versioning. No tag yet — wait until M1 lands or the Relay app earns it.
