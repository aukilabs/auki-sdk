[package]
name = "auki-session"
version = "0.0.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Path helpers for the Auki SDK on-disk session shape — single source of truth for app root, session, and per-recording paths."

[dependencies]
