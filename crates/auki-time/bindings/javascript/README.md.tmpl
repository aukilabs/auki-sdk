# @auki/$crate_package_name

wasm-bindgen JavaScript bindings for `auki-time`.

The wasm package exposes JSON-string adapters for web-safe time math:
NTP sample computation, best-sample selection, peer-clock sync state, and
domain-clock composition. Native host clock access and filesystem-backed
sampler threads are intentionally not exported to browser wasm.
