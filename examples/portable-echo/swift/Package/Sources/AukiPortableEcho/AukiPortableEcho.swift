/// Marker for the Swift module that combines the generic Auki peer facade and
/// the shared Rust portable-echo endpoint in one Apple artifact.
public enum AukiPortableEchoModule {
    public static let protocolId = "/example/echo/1.0.0"
}
