# auki-raster

Y-plane occupancy slice of an [`auki-geometry`](../auki-geometry) `TriangleMesh` → PNG + map.yaml.

```text
cross_section(mesh, height, ppm) -> CrossSection
```

Not nav bake (`auki-navigation`) and not raycast (`auki-geometry`).
